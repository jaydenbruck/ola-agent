# Copy this file to server/.env. Never commit live values.

# OpenRouter API key used for model requests. Required for real-model runs.
OPENROUTER_API_KEY=
# OpenRouter model identifier. Override to choose another supported model.
OLA_MODEL=x-ai/grok-4.5
# Shared bearer token. Generate a random value and enter the same value in the app.
OLA_TOKEN=
# Gmail account used to send mail through SMTP and read mail through IMAP.
OLA_MAIL_USER=
# Gmail app password for that account. Keep this value private.
OLA_MAIL_APP_PASSWORD=
# Google OAuth refresh token with the Calendar permissions used by the server.
OLA_GOOGLE_REFRESH_TOKEN=
# Google OAuth client ID associated with the refresh token.
OLA_GOOGLE_CLIENT_ID=
# Google OAuth client secret associated with the refresh token.
OLA_GOOGLE_CLIENT_SECRET=
